#include "connection.h"
void connection::Addrecord(person per)
{
    connection::Addrecord(per);
}
void connection::printAllrecords()
{
    for (person p : connection::lst)
    {
        std::cout << p.
    }
}